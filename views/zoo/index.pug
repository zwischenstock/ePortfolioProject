doctype html
html
  include ../includes/header.pug
  body
    ul
      each animal in animals
        li= animal.name + " (" + animal.species + ", " + animal.animal + ")"
    include ../includes/bodyend.pug